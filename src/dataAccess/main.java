package dataAccess;

import java.util.List;

public class main
{
    public static void main(String[] args)
    {
        /*RecipeDAO recipeDAO = new RecipeDAO();
        List<Recipe> recipes = recipeDAO.getAllRecipes();

        if (recipes != null && !recipes.isEmpty()) {
            recipes.forEach(recipe -> System.out.println(recipe.getLabel()));
        } else {
            System.out.println("Aucune recette trouvée.");
        }

        Recipe recipe = recipeDAO.getRecipeById(2);
        if (recipe != null) {
            System.out.println("Recette trouvée : " + recipe.getLabel());
        } else {
            System.out.println("Recette non trouvée.");
        }
        int result = recipeDAO.updateRecipe(3, "Nouvelle Recette", "Description mise à jour", 500, true, null, 30, 1);
        if (result > 0) {
            System.out.println("Recette mise à jour avec succès.");
        } else {
            System.out.println("Échec de la mise à jour de la recette.");
        }

        result = recipeDAO.deleteRecipe(2);
        if (result > 0) {
            System.out.println("Recette supprimée avec succès.");
        } else {
            System.out.println("Échec de la suppression de la recette.");
        }*/

    }
}
